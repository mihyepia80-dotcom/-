{
  "name": "midterm-eval-app",
  "private": true,
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "node scripts/generate-env.js",
    "dev": "node scripts/generate-env.js && node scripts/dev-server.js",
    "start": "node scripts/generate-env.js && node scripts/dev-server.js",
    "check-env": "node scripts/check-env.js",
    "admin-key": "node scripts/generate-admin-key.js",
    "firebase:deploy": "firebase deploy --only firestore"
  },
  "dependencies": {
    "firebase-admin": "^13.0.2"
  },
  "devDependencies": {
    "dotenv": "^16.4.7"
  }
}
